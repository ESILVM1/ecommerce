# Main docker-compose file for full-stack application
# This orchestrates both backend and frontend services

version: '3.8'

services:
  # Backend services are defined in backend/docker-compose.yml
  # Run with: docker-compose -f backend/docker-compose.yml up
  
  # Frontend will be added here when implemented
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend:/app
  #   depends_on:
  #     - backend

# For now, navigate to backend/ and run:
# docker-compose up --build
